<%= form_for(@travel_log) do |f| %>
  <% if @travel_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@travel_log.errors.count, "error") %> prohibited this travel_log from being saved:</h2>

      <ul>
      <% @travel_log.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <input type="hidden" name="travel_log[car_id]" value="1" />
  <div class="field">
    <%= f.label :odometer %><br />
    <%= f.text_field :odometer %>
  </div>
  <div class="field">
    <%= f.label :date %><br />
    <%= f.text_field :date, :class=> :datepicker %>
    <!--<%= f.date_select :date %>-->
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

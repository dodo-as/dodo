<%= form_for([:admin,@county]) do |f| %>
   
  <%= f.error_messages %>

  <p>
    <%= f.label :name %>
    <%= f.text_field :name %>
  <p>   
  
  <div class="actions">
   <%= f.submit %>
  </div>

<% end %>

<h1><%= t(:listing_tax_zones, :scope => :tax_zones) %></h1>

<table>
  <tr>
    <th>Number</th>
    <th></th>
  </tr>

<% @tax_zones.each do |tax_zone| %>
  <tr>
    <td><%= tax_zone.number %></td>
    <td><%= link_to 'Show', [:admin, tax_zone] %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Tax zone', new_admin_tax_zone_path %>
